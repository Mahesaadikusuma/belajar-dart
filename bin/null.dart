void main(List<String> args) {
  int? number1 = 1;
  int? number2;

  // Tanda ? itu untuk pengecekan value dari variabel yang tidak ada atau kosong jika
  // tidak menggunakan maka dart akan error

  // null dalam dart adalah object jadi sebelum ada valuenya maka pakai number2
  print(number1);
  print(number2);
}
